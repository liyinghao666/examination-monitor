addEventListener('message', (message) => {
  console.log(message)
  // window.Monitor.issueDetector.detect(webcam)
  // window.Monitor.poseDetect.detect()
  // window.Monitor.faceSeeker.seek()
})